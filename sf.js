function simpleFooter(sfename, sfdebug) {
  //REQUIRES JQUERY!!!
  var sfdHeight = $( document ).outerHeight();
  if (sfdebug == true) {
    console.log(sfdHeight.tostring);
  }
  document.getElementById(sfename).style.top = ((mama).toString() + "px");
}
